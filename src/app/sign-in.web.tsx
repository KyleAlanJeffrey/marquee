import { AuthScreen } from '@/components/auth-screen.web';

/** The sign-in half of the web auth screen — see components/auth-screen.web. */
export default function SignInScreen() {
  return <AuthScreen mode="sign-in" />;
}
